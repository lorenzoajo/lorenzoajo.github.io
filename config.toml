# The URL the site will be built for
base_url = "https://localhost:8080"
# base_url = "https://lorenzoajo.github.io/"#TODO remove this and put this line back

title = "Personal site cause why not"
description = "To lazy to describe my personal website"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

# The default language; used in feeds.
default_language = "en"

[extra]
# User settings
username = "Lorenzo Ajo"
user_description = "Extra description"
user_title = "Software Developer"
email = "lorenzo.ajo@gmail.com"
github_username =  "lorenzoajo"

# Google-analytics
google_analytics.id = ""
